import { adminNavs } from "./adminNavs";
import { landlordNavs } from "./landlordNavs";
import { tenantNavs } from "./tenantNavs";

export const sidebarNavs = {
    ADMIN: adminNavs,
    LANDLORD: landlordNavs,
    TENANT: tenantNavs,
}
